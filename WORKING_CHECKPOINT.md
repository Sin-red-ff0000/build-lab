# BUILD LAB Working Checkpoint — 2026-09-15 / phase v0.48

正式配布版ではない作業チェックポイント。基準は v0.36 完全版からの継続改修。

## 今回追加: 未使用・下位互換カード 第1次再設計
- 12枚を単純な数値強化ではなく役割分離。
- 双撃: 次の1hit攻撃への橋渡し。
- 三段斬り: 次の防御札へ防御を渡す連撃→防御ブリッジ。
- 針雨: 5hitを次の状態異常付与準備へ変換。
- 連防: 多段攻撃後の受け札。
- 紅の集中: 汎用+80%を撤廃し、次の自傷札だけ+35%。
- 血毒/供犠連撃: HP変動・再踏込みエンジンへ接続。
- 毒針連射: 毒の定着維持。
- 火針連射: 再点火へ接続。
- 防壁: 無条件安定防御として最初の敵攻撃だけ追加防御。
- 受け流し: 完全防御→回収→次防御への橋渡し。
- 症状爆砕: 状態異常の総量を丸ごと火力へ変える札から、症状維持を兼ねる利用札へ変更。

## テスト
- balance48_unused_cards.test.js: PASS
- history39/history40/position44/link45/tuning46/build47: PASS
- 通常回帰は expansion34_core_builds.test.js の旧極限試験「捨て札 0% < 70%」で意図的に停止。
  インフレ正常化後のカードを再び巨大化して旧試験を通すことはしない。

## 次
未使用・下位互換カード第2次。特に後発カードに役割ごと食われている攻撃/防御/循環札を、保持・単発重撃・防御消費・状態異常分化の受け皿へ再配置する。

## Roadmap 1/10 — v0.49 common event foundation (2026-09-15)
Implemented the first of the ten planned overhaul passes. Battle now owns a common event history (`events`) with global counts, per-card-instance counts and a bounded recent-event stream. Core events wired in this pass: `presented`, `selected`, `notChosen`, `used`, `reshuffle`, `selfDamage`, `heal`, `enemyDamage`, `statusApplied`, `enemyAction`. The public battle API also exposes `emitEvent`, `eventCount`, and `recentEvents`, so later systems can add `vanished`, `returned`, `transformed`, `defenseSpent`, etc. without inventing separate history stores.

This pass intentionally adds no large card batch and no power inflation. It is infrastructure for roadmap 2/10 (vanish / not-chosen / while-presented). `tests/events49_core.test.js` locks prompt events, per-instance histories and extension events such as `transform`.

Known red test remains the pre-existing `expansion34_core_builds.test.js` numerical endgame calibration after the v0.43 inflation normalization; do not restore oversized card values merely to satisfy it.

## Roadmap 2/10 完了 — v0.50 消失 / 非選択 / 提示中
- v0.49 共通イベント基盤を実際のカード機構へ接続。
- `vanished` を `excluded` と分離。消失カードは通常の山札再構築では戻らない。
- `vanished` / `returnedFromVanish` / `whilePresented` をイベント履歴へ記録。
- 非選択はカード実体UIDごとの `notChosen` 回数を参照可能。単なる「捨て札になった」条件と区別した。
- 提示中はカードを選ぶ前に小さな受動効果を発生可能。1ターン1枚選択という制約そのものをビルド資源にする。
- 新規9カード: 消失刃 / 消失障壁 / 空隙回帰 / 伏せた刃 / 見送る盾 / 眠り火 / 監視板 / 照準待機 / 毒見の標。
- 総カード数 649 → 658。
- `mechanics50_vanish_notchosen_presented.test.js` PASS。既存 `events49_core.test.js` PASS。
- 全回帰は今回追加分を通過後、既知の旧 `expansion34_core_builds.test.js`「捨て札型 0% < 70%」で停止。インフレカードを戻して解消しない。

## 次: Roadmap 3/10
- 記録 / 予約 / 変質 / 消失からの復帰を共通イベント基盤へ本格接続。
- 特に「他カードの性質や履歴を保存して後から使う」記録を、単純コピーではなく選択判断になる形で実装する。

## Roadmap 4/10 完了 — v0.52 二面カード本格拡張
- 二面カードを16種→28種へ拡張。新規12種は v0.49〜0.51 の共通イベント/履歴基盤を変身条件として利用。
- 新規条件: 実体ごとの非選択2回、消失からの復帰、予約到着2回、中央連続3回、左右交互2回、HP増減方向2回切替、毒/火傷3回付与、連結2回、再構築2回、記録再演、敵4行動。
- 表→裏を単純な数値強化にしない。防御→攻撃、供給→維持、自己予約→連撃、消失攻撃→復帰防御など、変身で役割・運用が変わる設計。
- 裏面の攻撃/防御は30未満に固定する専用監査を追加。条件達成だけで巨大数値を得る旧設計へ戻さない。
- 新規12枚は4つの可視アンロックへまとめ、全カードに解放経路を用意。
- `dual52_roles.test.js` PASS。既存二面総数固定テストも28へ更新。
- 全回帰は新規テスト通過後、既知の旧 `expansion34_core_builds.test.js`「捨て札型 0% < 70%」で停止。今回由来の新規停止なし。

## 次: Roadmap 5/10
新ビルド/キャラクター/スタイル大拡張。消失・非選択/提示中・記録/再現・敵行動利用・デメリット反転・変質から、独立した構築判断を持つ軸だけを正式な受け皿として追加する。

### Roadmap 5/10 完了 — 新ビルド生態系
新6キャラクター＋18スタイルを追加。消失/復帰、非選択/提示中、記録/再現、敵行動利用、デメリット反転、変質/二面を正式な横方向ビルドとして受け止める。次は6/10：カード全件再設計・前半。新旧カードを未使用/下位互換/役割圧縮/受け皿不足で再分類し、新生態系へ再配置する。

### Roadmap 6/10 完了 — カード全件再設計・前半
既存51カードを先行再設計。hit数・条件倍率・瀕死値・再構築回数だけで上位下位が決まる札を減らし、非選択、単発重撃への橋渡し、毒/火傷/出血/凍傷、循環/予約/復帰、敵行動利用などへ役割を分配した。カード本文に新しい語句を書くだけで終わらないよう、条件付きダメージ/ヒット/予約/敵挙動状態異常も battle.js に実装済み。

監査: 実ロード679カード、今回再設計51、完全同一説明0。専用テストと旧既知失敗3本を除く全テストPASS。既知失敗は v0.53 時点から存在する `expansion34_core_builds`（旧数値極限試験）、`chapter_boss_progression`（boss3 13.8%）、`endgame_all_styles`（旧141固定に対し現177構成）の3本。6/10ではこれらを巨大数値へ戻して通していない。

次: Roadmap 7/10 カード全件再設計・後半。後半解放カード、新規拡張カードを中心に、役割圧縮・高採用率候補・条件未達でも強い札を再監査し、新生態系へ再配置する。

### Roadmap 7/10 完了 — カード全件再設計・後半（v0.55）
後半解放の中心だった v0.35 系48カード（提示位置/連結/調律・変換/構築規格/敵解析/ルーン・アルカナ/元素/錬成を各6枚）を全件再設計。旧来の「条件成立で1.5〜1.7倍になり、どの系列も攻撃＋防御の高効率札になる」構造を撤去し、位置→重撃/状態異常、連結→循環/毒維持、解析→敵行動/出血、五元素→火傷/予約/回復循環/防御列/異軸接続、錬成→生成/在庫/反応/消費という別々の出口へ分配した。

`D.V35_CARD_RULES` の旧巨大倍率はカード固有分について撤去し、倍率条件を満たすだけで後発札が旧カードを食う状態を防止。48枚すべて無条件総ダメージ30未満、防御20未満を専用テストで固定。`AUDIT_v0.55_CARD_BACK.json` では実ロード679カード、今回48枚、完全同一説明0件。

全65テストを個別実行し、62 PASS / 既知3 FAIL。FAILは v0.54以前から残る `expansion34_core_builds`（旧数値極限試験）、`chapter_boss_progression`（旧boss3校正）、`endgame_all_styles`（旧141固定）だけで、v0.55由来の新規FAILなし。

次: Roadmap 8/10 遺物・プロトコル・調律の全件再設計。単純な +% 系列や同じ条件の数値違いを優先して、カードのルール/提示/消失/記録/状態異常/循環を変える横方向効果へ移す。

## Roadmap 8/10 完了 — v0.56 遺物・プロトコル・調律 全件監査/正常化
- 実ロード総数: 遺物507 / プロトコル175 / 調律82 = 764定義を全件走査。
- 後期拡張の条件達成報酬が +20〜70% と縦に伸びていたルールを全件監査し、569箇所を最大+12%へ正常化。未達ペナルティも原則-12%以内へ整理。
- 764定義すべてへ v0.56Role を付与し、選択・提示 / 循環・復帰 / 症状運用 / HP履歴 / 接続履歴 / 防御運用 / 役割変換 / 資源工程 / 条件選択の9受け皿へ分類。
- expansion28 の旧「条件が重いほど+50〜70%を要求する」テストは、今回の横方向設計と矛盾するため新基準へ移行。
- `AUDIT_v0.56_SYSTEM_ECOLOGY.json` と `balance56_system_ecology.test.js` を追加。
- 全66テスト個別実行: 63 PASS / 既知3 FAIL。既知FAILは boss3旧章校正13.8%、旧141構成固定、旧極限数値試験の捨て札0%。
- 次は9/10: 章・アンロック・報酬の全面再校正。今回正常化したシステムを「その章で理解して使う意味」が出るように進行側を作り直す。

## Roadmap 9/10 完了 — v0.57 章・アンロック・報酬の全面再校正
- 章進行を「その章で新システムを学ぶ → 実戦課題で運用 → ボスが卒業試験 → 次章の操作体系を報酬として解放」に統一。
- 第3ボス挑戦条件から極相個体撃破を撤去。連結ビルドII / 調律連結 / 連結循環の実運用を卒業条件にした。極相個体は任意高難度課題へ。
- 第4ボス挑戦条件から極限個体(v19_omega)撃破を撤去。圧縮・展開・単独の3構築規格を実戦で使い、統合実験を達成することを卒業条件にした。
- v09高度特殊個体は第1ボス直後表示から第2ボス後（第3章）へ移動。
- v20高難度特殊個体/複合挙動は第4ボス後の第9章研究へ隔離。HP×12/攻撃×10/防御×10/速度×4は第10章エンドコンテンツ研究へ移動。
- ボス報酬の役割を明文化: Boss1→提示操作/高度敵、Boss2→連結/役割変換、Boss3→構築規格、Boss4→クリア後ルーン/アルカナ。
- 旧 chapter_boss_progression の「固定AI構築で勝率差を出す」テストを、章カリキュラムと次章ブリッジを検証するテストへ更新。カードを再インフレさせて勝率を合わせる設計を廃止。
- `progression57_curriculum.test.js` / `AUDIT_v0.57_PROGRESSION.json` 追加。
- 全テスト再走査で、9/10由来の進行FAILは解消。残る既知FAILは旧141構成固定 `endgame_all_styles` と旧HP×12数値試験 `expansion34_core_builds` の2本のみ。どちらも10/10で新エンドコンテンツ基準へ置換する。

## 次: Roadmap 10/10
エンドコンテンツ＋特殊個体の再構築。単一のHP×12/攻撃×10/防御×10/速度×4試験ではなく、速攻・覚醒・多行動・消耗・状態異常管理・防御運用・デッキ干渉等の複数試験を用意し、全キャラ/全スタイルを「同じ万能札」ではなく各コンセプトで検証する。旧141固定テストも現行構成数を動的取得する最終監査へ置換する。

## Roadmap 10/10 完了 (v0.58)
公式エンドコンテンツを8種類の校正へ再構築。現在の49キャラクター＋128スタイル＝177構成を動的監査し、全構成に2つ以上の攻略経路、うち最低1つはコンセプト直接一致を要求する。旧HP×12/攻撃×10/防御×10/速度×4は任意研究へ降格。ロードマップ1/10〜10/10の主要改修は完了。次段階は最終全件監査（採用率0%、異常高採用率、万能遺物、状態異常格差、新旧カード格差、章攻略、8校正の偏り）と必要な修正。


## 最終監査修正 1/4 — v0.59
正式版前の収束作業。v0.58最終監査で判明したカード品質問題へ着手。v0.34の旧インフレ説明24枚を実処理へ同期し、連撃・攻防複合の主要支配系列を「hit数違い」ではなく別の役割出口へ再設計した。専用回帰テストを追加し、全チェックPASS。まだ正式版ではない。次は残存候補の全件再監査と再設計を行う。

## v0.60 最終監査修正 2/4
- 最終監査の42支配候補は15枚の連撃/攻防複合系列に集中。v0.59で役割分離済みのため、その構造を維持しつつ実処理漏れ（七重穿ちの消失）を修正。
- 60類似テンプレート群を再走査し、特に単純な数値階段になりやすい11系列43枚を横方向へ再設計。
- 同型条件でも、防御継続・状態異常準備・循環・安定・単発支援・消失・予約など採用理由を分離。
- 次回は実戦探索器を作り、177構成×8校正をタグ割当ではなく戦闘結果で測定する。

## 最終監査修正 3/4 — v0.61
実戦探索器を導入し、177構成×8校正=1,416戦を `BL.Battle` で実行。175/177構成は少なくとも1校正で勝利したが、ゼファー・霧流型とリスク原型は0勝。校正別では消耗175勝に対し先陣/浄化/統合0勝、群動3勝、空隙6勝で大きな難度差を確認。勝利構築のカード/遺物/プロトコル偏重、毒・火傷に対する出血・凍傷の低利用も検出。次回4/4で実測調整＋複数シード再監査を行う。

## v0.62 / 最終監査修正4
実戦再校正を実施。177/177構成が最低1校正で実勝利したが、先陣・群動・浄化がほぼ全構成で勝てるため専門性不足。次回は複数シード実戦＋採用率上限＋校正専門性を最終ゲートとする。正式版はまだ出さない。

## v0.63 最終監査修正5
- 万能化した散開破片と災厄プロトコルを役割分離。
- 校正の過易化を再調整。
- 最終PASS条件を3シード以上の実戦探索へ引き上げ。単一シードの177/177だけでは完成扱いしない。
- 次工程: 複数シード実戦の完走・採用率/状態異常/校正専門性の最終収束。正式版はまだ出さない。

## v0.64 最終監査修正6
複数シード監査へ移行。監査AIがスタイル固有のデッキ制約を破ると「弱い構成」を誤検出するため、まず孤響型の1枚制約を修正した。3シード監査を完成条件として固定。部分監査では消耗校正・凍傷軸に警告が残るため正式完成扱いにはしない。

## v0.65 最終監査修正7
- v0.64で判明した監査AIの構築バイアスを一般化して修正。
- スタイル固有のデッキ要件を V65_AUDIT_PROFILE に分離。
- 最終監査は hybrid / concept / trial × 3シード。最終PASSはまだ未宣言。

## v0.66 最終監査修正8
長時間の177構成×8校正×複数構築×3seed監査を、1構成ごとに永続化して再開できる方式へ変更。177件すべて揃うまで最終集計を拒否する。部分結果でゲーム側を調整しない。

## v0.67 最終監査修正9
- v0.66で作った再開可能監査を実際に進行。現在 51/177 構成の結果を `AUDIT_v0.66_PARTS` に保存済み。
- `tools/endgame67_parallel_resume.js` を追加し、完了済み結果を保持したまま複数workerで監査を継続可能にした。
- 51/177は部分結果なので、消耗・凍傷・万能札などのバランス判断にはまだ使用しない。
- 最終判定条件は変わらず177/177完走→統合監査→必要なゲーム側修正→再監査。

## v0.68 最終監査修正10
- 再開可能・並列監査を継続し、保存済み結果を 51/177 → 81/177 構成まで進行。
- 30構成分を新規完了。`AUDIT_v0.66_PARTS` のvariant JSONを次回へそのまま引き継ぐ。
- 177/177未満なので、消耗・凍傷・万能札などの部分傾向を根拠にゲーム性能を変更しない原則を維持。
- 通常全回帰は All checks passed。
- 次回は残り96構成を継続。177/177完走後のみ統合監査へ進む。

## v0.69 最終監査修正11 (2026-09-16)
- 177構成の再開可能・3方針×3シード×8校正実戦監査を継続。
- 完了済み構成を v0.68 の81件から128件へ拡張（+47件）。
- 部分監査結果だけではゲーム性能を変更しない方針を維持。
- 残り49構成。177/177完走後のみ統合監査を生成し、万能札・採用率0%・校正格差・状態異常格差を確定する。

## v0.70 最終監査修正12
177/177構成の完全3シード監査が完走。1416構成×校正結果を統合済み。174/177に勝利経路。未勝利はリレー・順送型、ブレイド・輪転継電型、コンバータ・消費型。消耗校正11/177、凍傷を含む勝利構築0件を確定問題とした。次回は監査AI補正後の未勝利3構成再検証と、凍傷・消耗校正のゲーム側調整を行う。

## Current checkpoint
- v0.72 final audit fix 14
- v0.71-based full re-audit: 16/177 complete
- Do not tune from partial results.

## v0.73 最終監査修正15 (2026-09-16)
- v0.71基準の完全再監査を16/177から48/177へ進行（+32）。
- 残り129構成。完了済み構成は `AUDIT_v0.66_PARTS` から再利用し再計算しない。
- 部分結果を理由としたゲーム性能変更はなし。
- 177/177完走後のみ統合監査・最終PASS判定を行う。

## Current checkpoint
- v0.73 final audit fix 15
- v0.71-based full re-audit: 48/177 complete
- Do not tune from partial results.
