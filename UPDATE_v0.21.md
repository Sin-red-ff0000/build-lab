# v0.20 → v0.21 更新手順

1. GitHub更新用ZIPを展開します。
2. 中身を、既存の index.html があるフォルダへそのまま上書きしてください。新しい js/data/alchemy.js、js/core/alchemy.js、js/ui/alchemyView.js も必要です。
3. デプロイ完了後に再読み込みし、タイトルが v0.21 になったことを確認します。
4. デッキ → 元素・錬成 → 構成例から試せます。

同じサイト・ブラウザのv0.20セーブを自動移行します。旧セーブは錬成未使用で読み込むので、進行を保持したまま試せます。全データ初期化は必要ありません。
完全版は全ファイル入りです。差分版はv0.20完全版を基準とし、ファイル削除はありません。

検証：bash tests/run_checks.sh または tests\run_checks.bat
