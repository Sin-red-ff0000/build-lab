# BUILD LAB v0.61 最終監査修正 3/4 — 実戦探索一次監査

## 目的
v0.58 の「177構成に攻略タグが割り当てられている」という静的検査を、実際の `BL.Battle` 戦闘結果で検証するための探索基盤へ置き換える。

## 実装
- `tools/endgame61_combat_audit.js` を追加。
- 実データから 49キャラクター + 128スタイル = 177構成を動的取得。
- `ENDGAME58` の8公式校正すべてに対し、各構成ごとにコンセプト＋校正回答から10枚デッキを自動構築。
- 全アンロック前提で、関連する遺物3枠・プロトコル・調律を探索器が選択。
- カード選択AIは、カードの基礎出力だけでなく構成コンセプト・校正回答・現在HP・敵残HPを参照。
- 実際の `BL.Battle.create()` / `BL.Battle.play()` を使って戦闘し、最大70ターンまで追跡。
- 一次監査は比較速度を優先して各「構成×校正」1固定シード。最終4/4では問題修正後に複数シードへ拡張する。

## 一次実測
- 177構成 × 8校正 = **1,416実戦**。
- 1つ以上の校正で勝利: **175 / 177構成**。
- 0勝: **ゼファー・霧流型 / リスク・原型**。
- 全8校正勝利: **0構成**。万能構築一択にはなっていないが、校正間格差が極端。

### 校正別「少なくともこの自動構築で勝てた構成数」
- 先陣校正: **0 / 177**
- 覚醒校正: **58 / 177**
- 群動校正: **3 / 177**
- 消耗校正: **175 / 177**
- 浄化校正: **0 / 177**
- 適応校正: **7 / 177**
- 空隙校正: **6 / 177**
- 統合校正: **0 / 177**

この結果から、v0.58 の静的ルート割当だけでは実戦難度を保証できていなかったことを正式確認。特に消耗校正が著しく易しく、先陣・浄化・統合が一次探索では突破不能。

## 採用偏重の検出
勝利構築では `v34_s_plague`、`v34_d_salvage_guard`、`v34_m_statusburst`、`v34_c_reset` が突出。遺物では `cracked_shield`、`cycle_bearing`、`recovery_thread`、プロトコルでは `cycle_prime` が大きく偏った。

状態異常を含む勝利構築数も毒/火傷が各249に対し、出血7、凍傷0。一次探索AIの選択バイアスを含むため即ナーフ/バフ判定はしないが、**出血・凍傷の受け皿不足またはカード評価不足を次回の重点対象**とする。

## 判定
v0.61 は「全構成がエンドコンテンツを突破可能」とは判定しない。今回の成果は、静的タグ検査では隠れていた実戦上の偏りを再現可能な形で可視化したこと。
次回3→4回目の修正では、校正難度、突出採用札、0勝構成、状態異常格差を実測値から調整し、その後複数シード再監査を行う。
