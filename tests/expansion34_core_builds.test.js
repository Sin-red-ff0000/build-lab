'use strict';
const fs=require('fs'),path=require('path'),assert=require('assert');global.window=global;global.localStorage={getItem(){return null},setItem(){},removeItem(){}};const root=path.resolve(__dirname,'..'),html=fs.readFileSync(path.join(root,'index.html'),'utf8');for(const m of html.matchAll(/<script src="([^"?]+)/g)){const f=m[1];if(f.includes('/ui/')||f.endsWith('app.js'))continue;require(path.join(root,f));}const D=BuildLab.Data,A=D.V34_BUILD_AXES,E=D.ENDGAME58;assert(A&&E);
for(const [id,a] of Object.entries(A)){assert.equal(a.cards.length,6);assert.equal(a.relics.length,6);for(const x of a.cards)assert(D.CARDS[x]);for(const x of a.relics)assert(D.RELICS[x]);const tag=a.tag;const compatible=Object.values(E.trials).filter(t=>t.answers.includes(tag)||t.answers.some(x=>String(tag).includes(x)||x.includes(String(tag))));assert(compatible.length>=1,`${id}/${tag}: no official endgame niche`);}
assert(E.legacyExtreme.required===false,'legacy extreme target became mandatory again');console.log('PASS expansion34_core_builds.test.js: six foundation axes have official endgame niches; raw HPx12 is optional');
