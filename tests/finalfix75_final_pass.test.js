'use strict';
const fs=require('fs'),path=require('path');global.window=global;global.localStorage={getItem(){return null},setItem(){},removeItem(){}};const root=path.resolve(__dirname,'..'),html=fs.readFileSync(path.join(root,'index.html'),'utf8');for(const m of html.matchAll(/<script src="([^"?]+)/g)){const f=m[1];if(f.includes('/ui/')||f.endsWith('app.js'))continue;require(path.join(root,f));}
const P=BuildLab.Data.V75_FINAL_AUDIT_PASS;if(!P||P.status!=='PASS'||P.blockingFindings.length)throw new Error('最終監査PASSが固定されていない');const a=JSON.parse(fs.readFileSync(path.join(root,'AUDIT_v0.75_ATTRITION_LOCAL_COMPLETE.json'),'utf8'));if(a.variants!==177||a.winsAnySeed<25||a.verdict!=='PASS'||a.enemyChangedFromV071)throw new Error('消耗校正局所収束監査が不正');console.log('finalfix75_final_pass.test.js: PASS');
