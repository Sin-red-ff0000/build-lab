'use strict';const fs=require('fs'),path=require('path');global.window=global;global.localStorage={getItem(){return null},setItem(){},removeItem(){}};const root=path.resolve(__dirname,'..'),html=fs.readFileSync(path.join(root,'index.html'),'utf8');for(const m of html.matchAll(/<script src="([^"?]+)/g)){const f=m[1];if(f.includes('/ui/')||f.endsWith('app.js'))continue;require(path.join(root,f));}const D=BuildLab.Data,A=D.V66_AUDIT_RUNNER;function ok(x,m){if(!x)throw new Error(m)}ok(A?.resumable&&A.perVariantCheckpoint,'監査は構成単位で再開可能');ok(A.totalVariants===177&&A.trials===8&&A.seeds===3,'177構成×8校正×3seed');ok(A.finalPassRequiresComplete177,'177構成完走前の最終PASS禁止');ok(A.partialResultsMustNotTriggerBalanceChanges,'部分結果でバランス変更禁止');const runner=fs.readFileSync(path.join(root,'tools/endgame66_resumable_audit.js'),'utf8');ok(runner.includes('spawnSync')&&runner.includes('AUDIT_v0.66_PARTS'),'構成単位チェックポイント実装');const merge=fs.readFileSync(path.join(root,'tools/endgame66_merge_audit.js'),'utf8');ok(merge.includes('files.length!==177'),'177件未満のマージ拒否');console.log('finalfix66 resumable audit passed');
