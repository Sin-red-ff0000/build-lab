'use strict';const fs=require('fs'),path=require('path');global.window=global;global.localStorage={getItem:()=>null,setItem:()=>{},removeItem:()=>{}};const root=path.resolve(__dirname,'..'),html=fs.readFileSync(path.join(root,'index.html'),'utf8');for(const m of html.matchAll(/<script src="([^"?]+)/g)){const f=m[1];if(f.includes('/ui/')||f.endsWith('app.js'))continue;require(path.join(root,f));}const D=BuildLab.Data;for(const [id,c] of Object.entries(D.CARDS)){let total=(c.damage||0)*(c.hits||((c.damage!=null)?1:0));if(c.kind==='damage'&&!c.status&&!c.selfDamage&&!c.armorPierce&&total<=12)console.log(id,c.name,total,c.desc)}
